@extends('layouts.app')
@section('content')
<section class="detail"><div class="detail-image" @if($product->image_url) style="background-image:url('{{ $product->image_url }}')" @endif><span>{{ $product->category->name }}</span></div><div class="detail-copy"><p class="eyebrow">{{ $product->category->name }} / {{ $product->color }}</p><h1>{{ $product->name }}</h1><p class="price">${{ number_format($product->price, 2) }}</p><p class="description">{{ $product->description }}</p><div class="specs"><span>Size <b>{{ $product->size }}</b></span><span>Stock <b>{{ $product->stock }} available</b></span><span>Curated by <b>{{ $product->user->name }}</b></span></div>@auth @can('update', $product)<div class="actions"><a class="button" href="{{ route('products.edit', $product) }}">Edit product</a><form method="POST" action="{{ route('products.destroy', $product) }}"><input type="hidden" name="_token" value="{{ csrf_token() }}"><input type="hidden" name="_method" value="DELETE"><button class="button button-danger" type="submit" onclick="return confirm('Remove this product?')">Delete</button></form></div>@endcan @endauth</div></section>
@endsection
